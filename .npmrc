# Suppress deprecation warnings
loglevel=error

# Use the new --omit=dev syntax instead of --production
production=false

# Suppress audit warnings for development
audit=false

# Suppress fund messages
fund=false

# Suppress update notifications
update-notifier=false
