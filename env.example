# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/localconnect
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=localconnect
DB_HOST=localhost
DB_PORT=5432

# Database Sync Options (use with caution)
# FORCE_SYNC=true  # Drops all tables and recreates them (DANGEROUS!)
# ALTER_SYNC=true  # Alters existing tables to match models

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=development
HOST=0.0.0.0

# Network Configuration for Mobile Apps
CORS_ORIGIN=*
FRONTEND_URL=*
MOBILE_TIMEOUT=10000
ENABLE_NETWORK_LOGGING=true

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Email Configuration (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# SMS Configuration (optional)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=your-twilio-phone-number

# Cloud Storage (optional)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Bcrypt Configuration
BCRYPT_ROUNDS=12

# Redis Configuration
REDIS_URL=redis://default:R3i8JWExclJVM8m6QSceneWSrWNlY03e@redis-14143.c240.us-east-1-3.ec2.redns.redis-cloud.com:14143
